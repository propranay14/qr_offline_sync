name: qr_offline_sync
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  mobile_scanner:
  camera:
  drift:
  dio:
  connectivity_plus: ^7.1.1
  flutter_bloc:
  path_provider:
  permission_handler: ^12.0.3
  flutter_blue_plus:
  sqflite: ^2.4.2+1
  fluttertoast:
#  usb_serial:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
